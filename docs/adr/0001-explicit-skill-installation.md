# Explicit Skill Installation

Paper Search CLI ships its Bundled Skill inside the npm package, but writing that Skill into user agent directories is an explicit user action. `paper-search setup` may offer Skill installation after showing Installation Destinations, and `paper-search skills update` may update selected Skill Targets; package installation itself does not modify user Skill directories.
