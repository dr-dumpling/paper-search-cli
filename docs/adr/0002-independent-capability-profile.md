# Independent Capability Profile

Paper Search CLI reports literature workflow readiness through independent Capability Profile entries such as `metadata_search`, `body_snippet_search`, `journal_metrics`, `pdf_discovery`, and `entitled_access`. Each entry carries its own status and Capability Reason, so missing credentials for one workflow are reported as Capability Degradation while other configured workflows remain usable.
